name: CodeQL 
on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
 code_analysis:
   uses: advanced-security-demo/central-repo-test/.github/workflows/code_analysis.yml@main
